PART 52/75
tValue(village, out var value) && value == this)
		{
			_activeHerders.Remove(village);
		}
	}

	internal static void AutoGeneratedStaticCollectObjectsHerderPartyComponent(object o, List<object> collectedObjects)
	{
		((HerderPartyComponent)o).AutoGeneratedInstanceCollectObjects(collectedObjects);
	}

	protected override void AutoGeneratedInstanceCollectObjects(List<object> collectedObjects)
	{
		base.AutoGeneratedInstanceCollectObjects(collectedObjects);
		collectedObjects.Add(OriginalClan);
	}

	internal static object AutoGeneratedGetMemberValueOriginalClan(object o)
	{
		return ((HerderPartyComponent)o).OriginalClan;
	}
}

===== END FILE =====

===== FILE: HarvestAndProduction\HarvestAndProduction\PartyComponents\NavalSupplyConvoyPartyComponent.cs =====
using System.Collections.Generic;
using Helpers;
using TaleWorlds.CampaignSystem;
using TaleWorlds.CampaignSystem.Actions;
using TaleWorlds.CampaignSystem.Party;
using TaleWorlds.CampaignSystem.Party.PartyComponents;
using TaleWorlds.CampaignSystem.Settlements;
using TaleWorlds.Core;
using TaleWorlds.Library;
using TaleWorlds.Localization;
using TaleWorlds.SaveSystem;

namespace HarvestAndProduction.PartyComponents;

public class NavalSupplyConvoyPartyComponent : PartyComponent
{
	public enum ConvoyState
	{
		PreparingAtHome,
		TravelingToDestination,
		UnloadingAtDestination,
		Returning,
		WaitingAtHome,
		Fleeing,
		HoldingOffshore,
		Idle
	}

	[CachedData]
	private TextObject _cachedName;

	[SaveableProperty(1)]
	public Settlement HomeTown { get; private set; }

	[SaveableProperty(2)]
	public Settlement DestinationTown { get; set; }

	[SaveableProperty(3)]
	public ConvoyState State { get; set; }

	[SaveableProperty(4)]
	public CampaignTime WaitStartTime { get; set; }

	[SaveableProperty(5)]
	public int FoodDelivered { get; set; }

	[SaveableProperty(6)]
	public int ShipsBorrowed { get; set; }

	[SaveableProperty(7)]
	public Clan OriginalClan { get; private set; }

	[CachedData]
	public MobileParty ThreatParty { get; set; }

	public override Hero PartyOwner
	{
		get
		{
			Clan clan = OriginalClan ?? HomeTown?.OwnerClan;
			if (clan == null || clan.IsEliminated)
			{
				return null;
			}
			return clan.Leader;
		}
	}

	public override Settlement HomeSettlement => HomeTown;

	public override bool CanHaveNavalNavigationCapability => true;

	public override bool CanHaveLandNavigationCapability => false;

	public override TextObject Name
	{
		get
		{
			if (_cachedName == null)
			{
				_cachedName = new TextObject("{=HAP_NavalConvoyName}Supply Convoy of {HOME_TOWN_NAME}");
				_cachedName.SetTextVariable("HOME_TOWN_NAME", HomeTown?.Name ?? new TextObject("{=HAP_SupplyFallbackUnknown}Unknown"));
			}
			return _cachedName;
		}
	}

	public override Banner GetDefaultComponentBanner()
	{
		if (OriginalClan?.Banner != null)
		{
			return OriginalClan.Banner;
		}
		return HomeTown?.OwnerClan?.Banner;
	}

	public override void GetMountAndHarnessVisualIdsForPartyIcon(PartyBase party, out string mountStringId, out string harnessStringId)
	{
		mountStringId = string.Empty;
		harnessStringId = string.Empty;
	}

	public override void ClearCachedName()
	{
		_cachedName = null;
	}

	protected NavalSupplyConvoyPartyComponent()
	{
	}

	protected NavalSupplyConvoyPartyComponent(Settlement homeTown, Settlement destinationTown)
	{
		HomeTown = homeTown;
		DestinationTown = destinationTown;
		OriginalClan = homeTown?.OwnerClan;
		State = ConvoyState.PreparingAtHome;
		WaitStartTime = CampaignTime.Never;
		FoodDelivered = 0;
		ShipsBorrowed = 0;
	}

	public static MobileParty CreateNavalSupplyConvoy(string stringId, Settlement homeTown, Settlement destinationTown)
	{
		return MobileParty.CreateParty(stringId, new NavalSupplyConvoyPartyComponent(homeTown, destinationTown));
	}

	protected override void OnMobilePartySetOnCreation()
	{
		base.MobileParty.Aggressiveness = 0f;
		base.MobileParty.ActualClan = OriginalClan;
		base.MobileParty.InitializePartyTrade(0);
		base.MobileParty.SetLandNavigationAccess(access: false);
		base.MobileParty.DesiredAiNavigationType = MobileParty.NavigationType.Naval;
		base.MobileParty.Ai.SetDoNotMakeNewDecisions(doNotMakeNewDecisions: true);
		base.MobileParty.SetPartyUsedByQuest(isActivelyUsed: true);
		base.MobileParty.Party.SetVisualAsDirty();
		if (HomeTown != null && HomeTown.HasPort)
		{
			base.MobileParty.InitializeMobilePartyAtPosition(HomeTown.PortPosition);
		}
		else if (HomeTown != null)
		{
			base.MobileParty.InitializeMobilePartyAtPosition(HomeTown.GatePosition);
		}
	}

	protected override void OnInitialize()
	{
	}

	protected override void OnFinalize()
	{
	}

	public void SetDestination(Settlement destination)
	{
		if (base.MobileParty != null && destination != null)
		{
			AiHelper.GetBestNavigationTypeAndAdjustedDistanceOfSettlementForMobileParty(base.MobileParty, destination, isTargetingPort: true, out var bestNavigationType, out var _, out var isFromPort);
			if (bestNavigationType == MobileParty.NavigationType.Default || bestNavigationType == MobileParty.NavigationType.None)
			{
				bestNavigationType = MobileParty.NavigationType.Naval;
				isFromPort = true;
			}
			SetPartyAiAction.GetActionForVisitingSettlement(base.MobileParty, destination, bestNavigationType, isFromPort, isTargetingPort: true);
		}
	}

	public void SetHoldPosition()
	{
		if (base.MobileParty != null)
		{
			base.MobileParty.SetMoveModeHold();
		}
	}

	internal static void AutoGeneratedStaticCollectObjectsNavalSupplyConvoyPartyComponent(object o, List<object> collectedObjects)
	{
		((NavalSupplyConvoyPartyComponent)o).AutoGeneratedInstanceCollectObjects(collectedObjects);
	}

	protected override void AutoGeneratedInstanceCollectObjects(List<object> collectedObjects)
	{
		base.AutoGeneratedInstanceCollectObjects(collectedObjects);
		collectedObjects.Add(HomeTown);
		collectedObjects.Add(DestinationTown);
		collectedObjects.Add(OriginalClan);
	}

	internal static object AutoGeneratedGetMemberValueHomeTown(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).HomeTown;
	}

	internal static object AutoGeneratedGetMemberValueDestinationTown(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).DestinationTown;
	}

	internal static object AutoGeneratedGetMemberValueState(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).State;
	}

	internal static object AutoGeneratedGetMemberValueWaitStartTime(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).WaitStartTime;
	}

	internal static object AutoGeneratedGetMemberValueFoodDelivered(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).FoodDelivered;
	}

	internal static object AutoGeneratedGetMemberValueShipsBorrowed(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).ShipsBorrowed;
	}

	internal static object AutoGeneratedGetMemberValueOriginalClan(object o)
	{
		return ((NavalSupplyConvoyPartyComponent)o).OriginalClan;
	}

	public static bool IsNavalSupplyConvoy(MobileParty party)
	{
		return party?.PartyComponent is NavalSupplyConvoyPartyComponent;
	}

	public static NavalSupplyConvoyPartyComponent GetComponent(MobileParty party)
	{
		return party?.PartyComponent as NavalSupplyConvoyPartyComponent;
	}

	public static bool IsAnySupplyParty(MobileParty party)
	{
		if (party?.PartyComponent == null)
		{
			return false;
		}
		return party.PartyComponent is NavalSupplyConvoyPartyComponent || party.PartyComponent is TownSupplyLinePartyComponent;
	}
}

===== END FILE =====

===== FILE: HarvestAndProduction\HarvestAndProduction\PartyComponents\TownSupplyLinePartyComponent.cs =====
using System.Collections.Generic;
using Helpers;
using TaleWorlds.CampaignSystem;
using TaleWorlds.CampaignSystem.Actions;
using TaleWorlds.CampaignSystem.Party;
using TaleWorlds.CampaignSystem.Party.PartyComponents;
using TaleWorlds.CampaignSystem.Roster;
using TaleWorlds.CampaignSystem.Settlements;
using TaleWorlds.Core;
using TaleWorlds.Library;
using TaleWorlds.Localization;
using TaleWorlds.SaveSystem;

namespace HarvestAndProduction.PartyComponents;

public class TownSupplyLinePartyComponent : PartyComponent
{
	public enum SupplyLineState
	{
		PreparingAtHome,
		TravelingToDestination,
		UnloadingAtDestination,
		Returning,
		WaitingAtHome,
		Fleeing,
		Defending,
		Idle
	}

	[CachedData]
	private TextObject _cachedName;

	[SaveableProperty(1)]
	public Settlement HomeTown { get; private set; }

	[SaveableProperty(2)]
	public Settlement DestinationTown { get; set; }

	[SaveableProperty(3)]
	public SupplyLineState State { get; set; }

	[SaveableProperty(4)]
	public CampaignTime WaitStartTime { get; set; }

	[SaveableProperty(5)]
	public int FoodDelivered { get; set; }

	[SaveableProperty(6)]
	public int PackAnimalsTaken { get; set; }

	[SaveableProperty(7)]
	public Clan OriginalClan { get; private set; }

	[CachedData]
	public MobileParty ThreatParty { get; set; }

	public override Hero PartyOwner
	{
		get
		{
			Clan clan = OriginalClan ?? HomeTown?.OwnerClan;
			if (clan == null || clan.IsEliminated)
			{
				return null;
			}
			return clan.Leader;
		}
	}

	public override Settlement HomeSettlement => HomeTown;

	public override bool CanHaveNavalNavigationCapability => false;

	public override TextObject Name
	{
		get
		{
			if (_cachedName == null)
			{
				_cachedName = new TextObject("{=HAP_SupplyLinePartyName}Supply Caravan of {HOME_TOWN_NAME}");
				_cachedName.SetTextVariable("HOME_TOWN_NAME", HomeTown?.Name ?? new TextObject("{=HAP_SupplyFallbackUnknown}Unknown"));
			}
			return _cachedName;
		}
	}

	public override Banner GetDefaultComponentBanner()
	{
		if (OriginalClan != null && OriginalClan.Banner != null)
		{
			return OriginalClan.Banner;
		}
		return HomeTown?.Banner;
	}

	public override void GetMountAndHarnessVisualIdsForPartyIcon(PartyBase party, out string mountStringId, out string harnessStringId)
	{
		mountStringId = string.Empty;
		harnessStringId = string.Empty;
		if (base.MobileParty == null)
		{
			return;
		}
		bool flag = false;
		bool flag2 = false;
		if (base.MobileParty.ItemRoster != null)
		{
			foreach (ItemRosterElement item2 in base.MobileParty.ItemRoster)
			{
				ItemObject item = item2.EquipmentElement.Item;
				if (item?.HorseComponent != null && item.HorseComponent.IsPackAnimal)
				{
					flag = true;
					if (item.StringId.Contains("camel"))
					{
						flag2 = true;
					}
				}
			}
		}
		if (flag)
		{
			bool flag3 = false;
			if (HomeTown != null && Campaign.Current?.MapSceneWrapper != null)
			{
				Vec2 getPosition2D = HomeTown.GetPosition2D;
				PathFaceRecord faceIndex = Campaign.Current.MapSceneWrapper.GetFaceIndex(new CampaignVec2(getPosition2D, isOnLand: true));
				TerrainType faceTerrainType = Campaign.Current.MapSceneWrapper.GetFaceTerrainType(faceIndex);
				flag3 = faceTerrainType == TerrainType.Desert || faceTerrainType == TerrainType.Dune;
			}
			if (flag3 & flag2)
			{
				mountStringId = "pack_camel";
				string[] array = new string[2] { "camel_saddle_a", "camel_saddle_b" };
				harnessStringId = array[MBRandom.RandomInt(array.Length)];
			}
			else
			{
				mountStringId = "mule";
				string[] array2 = new string[3] { "mule_load_a", "mule_load_b", "mule_load_c" };
				harnessStringId = array2[MBRandom.RandomInt(array2.Length)];
			}
		}
		else
		{
			mountStringId = string.Empty;
			harnessStringId = string.Empty;
		}
	}

	public override void ClearCachedName()
	{
		_cachedName = null;
	}

	protected TownSupplyLinePartyComponent()
	{
	}

	protected TownSupplyLinePartyComponent(Settlement homeTown, Settlement destinationTown)
	{
		HomeTown = homeTown;
		DestinationTown = destinationTown;
		OriginalClan = homeTown?.OwnerClan;
		State = SupplyLineState.PreparingAtHome;
		WaitStartTime = CampaignTime.Never;
		FoodDelivered = 0;
		PackAnimalsTaken = 0;
	}

	public static MobileParty CreateSupplyLineParty(string stringId, Settlement homeTown, Settlement destinationTown)
	{
		return MobileParty.CreateParty(stringId, new TownSupplyLinePartyComponent(homeTown, destinationTown));
	}

	protected override void OnMobilePartySetOnCreation()
	{
		base.MobileParty.Aggressiveness = 0f;
		base.MobileParty.ActualClan = OriginalClan;
		base.MobileParty.InitializePartyTrade(0);
		base.MobileParty.DesiredAiNavigationType = MobileParty.NavigationType.Default;
		base.MobileParty.Ai.SetDoNotMakeNewDecisions(doNotMakeNewDecisions: true);
		base.MobileParty.SetPartyUsedByQuest(isActivelyUsed: true);
		base.MobileParty.Party.SetVisualAsDirty();
		if (HomeTown != null)
		{
			base.MobileParty.InitializeMobilePartyAtPosition(HomeTown.GatePosition);
		}
	}

	protected override void OnInitialize()
	{
	}

	protected override void OnFinalize()
	{
	}

	public int GetPackAnimalCount()
	{
		if (base.MobileParty?.ItemRoster == null)
		{
			return 0;
		}
		int num = 0;
		ItemRoster itemRoster = base.MobileParty.ItemRoster;
		for (int i = 0; i < itemRoster.Count; i++)
		{
			ItemObject itemAtIndex = itemRoster.GetItemAtIndex(i);
			if (itemAtIndex != null && IsPackAnimal(itemAtIndex))
			{
				num += itemRoster.GetElementNumber(i);
			}
		}
		return num;
	}

	public static bool IsPackAnimal(ItemObject item)
	{
		if (item == null)
		{
			return false;
		}
		if (item.ItemCategory == DefaultItemCategories.PackAnimal)
		{
			return true;
		}
		if (item.HasHorseComponent && item.HorseComponent.IsPackAnimal)
		{
			return true;
		}
		return false;
	}

	public static bool IsRidingMount(ItemObject item)
	{
		if (item == null)
		{
			return false;
		}
		if (!item.HasHorseComponent)
		{
			return false;
		}
		if (!item.HorseComponent.IsMount)
		{
			return false;
		}
		return item.ItemCategory == DefaultItemCategories.Horse;
	}

	public int GetCarryingCapacity()
	{
		if (base.MobileParty == null)
		{
			return 0;
		}
		return base.MobileParty.InventoryCapacity;
	}

	public void SetDestination(Settlement destination)
	{
		if (base.MobileParty != null && destination != null)
		{
			AiHelper.GetBestNavigationTypeAndAdjustedDistanceOfSettlementForMobileParty(base.MobileParty, destination, isTargetingPort: false, out var bestNavigationType, out var _, out var isFromPort);
			if ((bestNavigationType & MobileParty.NavigationType.Naval) != MobileParty.NavigationType.None)
			{
				bestNavigationType = MobileParty.NavigationType.Default;
				isFromPort = false;
			}
			SetPartyAiAction.GetActionForVisitingSettlement(base.MobileParty, destination, bestNavigationType, isFromPort, isTargetingPort: false);
		}
	}

	public void SetDefendTown()
	{
		if (base.MobileParty != null && HomeTown != null)
		{
			base.MobileParty.SetMovePatrolAroundSettlement(HomeTown, MobileParty.NavigationType.Default, isTargetingPort: false);
		}
	}

	public bool CanReachByLand(Settlement destination)
	{
		if (base.MobileParty == null || destination == null)
		{
			return false;
		}
		AiHelper.GetBestNavigationTypeAndAdjustedDistanceOfSettlementForMobileParty(base.MobileParty, destination, isTargetingPort: false, out var bestNavigationType, out var _, out var _);
		return bestNavigationType != MobileParty.NavigationType.None && (bestNavigationType & MobileParty.NavigationType.Naval) == 0;
	}

	internal static void AutoGeneratedStaticCollectObjectsTownSupplyLinePartyComponent(object o, List<object> collectedObjects)
	{
		((TownSupplyLinePartyComponent)o).AutoGeneratedInstanceCollectObjects(collectedObjects);
	}

	protected override void AutoGeneratedInstanceCollectObjects(List<object> collectedObjects)
	{
		base.AutoGeneratedInstanceCollectObjects(collectedObjects);
		collectedObjects.Add(HomeTown);
		collectedObjects.Add(DestinationTown);
		collectedObjects.Add(OriginalClan);
	}

	internal static object AutoGeneratedGetMemberValueHomeTown(object o)
	{
		return ((TownSupplyLinePartyComponent)o).HomeTown;
	}

	internal static object AutoGeneratedGetMemberValueDestinationTown(object o)
	{
		return ((TownSupplyLinePartyComponent)o).DestinationTown;
	}

	internal static object AutoGeneratedGetMemberValueState(object o)
	{
		return ((TownSupplyLinePartyComponent)o).State;
	}

	internal static object AutoGeneratedGetMemberValueWaitStartTime(object o)
	{
		return ((TownSupplyLinePartyComponent)o).WaitStartTime;
	}

	internal static object AutoGeneratedGetMemberValueFoodDelivered(object o)
	{
		return ((TownSupplyLinePartyComponent)o).FoodDelivered;
	}

	internal static object AutoGeneratedGetMemberValuePackAnimalsTaken(object o)
	{
		return ((TownSupplyLinePartyComponent)o).PackAnimalsTaken;
	}

	internal static object AutoGeneratedGetMemberValueOriginalClan(object o)
	{
		return ((TownSupplyLinePartyComponent)o).OriginalClan;
	}

	public static bool IsSupplyLineParty(MobileParty party)
	{
		return party?.PartyComponent is TownSupplyLinePartyComponent;
	}

	public static TownSupplyLinePartyComponent GetComponent(MobileParty party)
	{
		return party?.PartyComponent as TownSupplyLinePartyComponent;
	}
}

===== END FILE =====

===== FILE: HarvestAndProduction\HarvestAndProduction\Patches\AiIssueSolvingPatch.cs =====
using System;
using System.Collections.Generic;
using System.Reflection.Emit;
using HarmonyLib;
using MCM.Abstractions.Base.Global;
using TaleWorlds.CampaignSystem;
using TaleWorlds.CampaignSystem.Actions;
using TaleWorlds.CampaignSystem.CampaignBehaviors;
using TaleWorlds.CampaignSystem.Issues;
using TaleWorlds.CampaignSystem.Party;
using TaleWorlds.CampaignSystem.Settlements;

namespace HarvestAndProduction.Patches;

public static class AiIssueSolvingPatch
{
	[HarmonyPatch(typeof(IssuesCampaignBehavior), "OnSettlementEntered")]
	public static class IssueSolvingChancePatch
	{
		[HarmonyTranspiler]
		public static IEnumerable<CodeInstruction> Transpiler(IEnumerable<CodeInstruction> instructions)
		{
			bool replacedFirst = false;
			bool replacedSecond = false;
			foreach (CodeInstruction instruction in instructions)
			{
				if (!replacedFirst && instruction.opcode == OpCodes.Ldc_R4 && instruction.operand is float f1 && Math.Abs(f1 - 0.05f) < 0.001f)
				{
					instruction.opcode = OpCodes.Call;
					instruction.operand = AccessTools.Method(typeof(AiIssueSolvingPatch), "GetClanOwnedChance");
					replacedFirst = true;
				}
				else if (replacedFirst && !replacedSecond && instruction.opcode == OpCodes.Ldc_R4 && instruction.operand is float f2 && Math.Abs(f2 - 0.01f) < 0.001f)
				{
					instruction.opcode = OpCodes.Call;
					instruction.operand = AccessTools.Method(typeof(AiIssueSolvingPatch), "GetOtherChance");
					replacedSecond = true;
				}
				yield return instruction;
			}
		}
	}

	[HarmonyPatch(typeof(NearbyBanditBaseIssueBehavior), "OnIssueUpdated")]
	public static class BanditHideoutDestructionPatch
	{
		[HarmonyPrefix]
		public static bool Prefix(IssueBase issue, IssueBase.IssueUpdateDetails details, Hero issueSolver)
		{
			HAPSettings instance = GlobalSettings<HAPSettings>.Instance;
			if (instance != null && !instance.EnableHideoutClearing)
			{
				return true;
			}
			if (details != IssueBase.IssueUpdateDetails.IssueFinishedByAILord)
			{
				return true;
			}
			if (!(issue is NearbyBanditBaseIssueBehavior.NearbyBanditBaseIssue obj))
			{
				return true;
			}
			try
			{
				Settlement settlement = (Settlement)AccessTools.Field(typeof(NearbyBanditBaseIssueBehavior.NearbyBanditBaseIssue), "_targetHideout").GetValue(obj);
				if (settlement?.Hideout == null)
				{
					return false;
				}
				List<MobileParty> list = ne